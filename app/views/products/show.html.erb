<div class="antialiased">
  
    <div class="py-6">

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-6">
        <div class="flex flex-col md:flex-row -mx-4">
        <div class="md:flex-1 px-4">
            <div x-data="{ image: 1 }" x-cloak>
            <div class="h-64 md:h-80 rounded-lg bg-gray-100 mb-4">
                <div x-show="image === 1" class="h-64 md:h-80 rounded-lg bg-gray-100 mb-4 flex items-center justify-center">
                  <% if @product.image.attached? %>
                    <img src="<%= url_for(@product.image) %>" class= "h-full w-full object-cover object-center lg:h-full lg:w-full">
                  <% end %>
                </div>
            </div>

            <div class="flex -mx-2 mb-4">
                <template x-for="i in 4">
                <div class="flex-1 px-2">
                    <button x-on:click="image = i" :class="{ 'ring-2 ring-indigo-300 ring-inset': image === i }" class="focus:outline-none w-full rounded-lg h-24 md:h-32 bg-gray-100 flex items-center justify-center">
                    <span x-text="i" class="text-2xl"></span>
                    </button>
                </div>
                </template>
            </div>
            </div>
        </div>
        <div class="md:flex-1 px-4">
            <h2 class="mb-2 leading-tight tracking-tight font-bold text-gray-800 text-2xl md:text-3xl"><%= @product.name %></h2>

            <p class="text-gray-500"><%= @product.description %></p>

            <div class="flex py-4 space-x-4">

            <%= button_to "Add to Cart", add_item_path(@product), class:"h-14 px-6 py-2 font-semibold rounded-xl bg-indigo-600 hover:bg-indigo-500 text-white" %>
            <%= link_to "Edit Item", edit_product_path(@product), class:"h-14 px-6 py-2 font-semibold rounded-xl bg-green-600 hover:bg-indigo-500 text-white" %>
            <%= button_to "Delete Item", product_path(@product), method: :delete, class:"h-14 px-6 py-2 font-semibold rounded-xl bg-red-600 hover:bg-indigo-500 text-white" %>
            </div>
        </div>
        </div>
    </div>
    </div>
</div>